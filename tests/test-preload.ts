/**
 * Bun test preload. Referenced by `bunfig.toml` ([test].preload). Runs once
 * before the test suite. Use this for global setup (e.g. registering mocks,
 * configuring fake timers) once the plugin grows real behavior. Intentionally
 * empty for the scaffold.
 */

export {};
